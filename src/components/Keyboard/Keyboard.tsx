import { useState } from 'react';
import classNames from 'classnames';
import styles from './Keyboard.module.scss';

const Keyboard = () => {
  return <div className={classNames(styles.root)}>Keyboard</div>;
};

export default Keyboard;
