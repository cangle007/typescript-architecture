import { useState } from 'react';
import classNames from 'classnames';
import styles from './HangmanWord.module.scss';

const HangmanWord = () => {
  return <div className={classNames(styles.root)}>HangmanWord</div>;
};

export default HangmanWord;
